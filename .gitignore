# dependencies
node_modules/

# wrangler / cloudflare
.wrangler/
.mf/

# Secrets locais (Wrangler dev) — nunca commitar
.dev.vars
.dev.vars.*
!.dev.vars.example

# Variáveis de ambiente
.env
.env.*
!.env.example

# Chaves, certificados e credenciais
*.pem
*.key
*.p12
*.pfx
*.secrets
secrets.json
credentials.json
**/secrets/

# Arquivos temporários de teste (login, tokens, etc.)
tmp-*.json
*.local.json

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# VS Code — config local com paths/secrets (launch.json genérico pode ir no repo)
.vscode/settings.json

# Artefatos de extração — referência local, não vão pro deploy/push
raw/
scripts/

# Mockups locais do companion visual
.superpowers/
